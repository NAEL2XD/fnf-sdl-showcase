# What's this?

FNF SDL SHOWCASE made in c++ and SDL3

it is built for less cpu usage and less calls, trying to make loading jsons very fast using simdjson and rendering without it clogging the gpu.

cmd to compile: `g++ main.cpp simdjson.cpp -Iinclude -L. -lSDL3 -O3 -march=native -fopenmp -funroll-loops -fprefetch-loop-arrays -fno-strict-aliasing -ffast-math -Wall -flto=auto -fdata-sections -Wl,--gc-sections -fopenmp -o main`

## THIS IS NOT AN ENGINE, IT'S A SHOWCASE.

Hey assholes, read this (actually you can't cause you're skipping to downloads like a baby), this repo is A SHOWCASE, which is meant for playback only.

an engine means it should have EVERYTHING for stuff (like h slice or js engine) which this repo is NOT supposed to be, it's like calling art an engine.

# IF YOU ASK FOR HELP THEN I'M NOT HELPING
